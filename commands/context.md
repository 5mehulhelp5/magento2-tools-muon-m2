---
description: Resolve the Magento 2 project context — vendor, runner, versions, theme, tools (context). Use when explicitly asked to resolve or refresh project context.
argument-hint: "[--no-cache]"
---
Use the `magento2-tools:context` skill to resolve and emit the project context. Forward these arguments verbatim: $ARGUMENTS
